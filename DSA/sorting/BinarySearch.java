public class BinarySearch {
    static int search(int[] arr, int x)
    {
        if(arr[0] == x)
            return 0;
        
        int n = arr.length;
        int index = n/2;
        int s = 0, e = n;

        if(arr[index] > x)

        if(arr[index] < x)
            index += ;
        if(arr[index] == x)
            return index;
        
        return index;
    }
    public static void main(String[] args) {
        int arr[] = { 2, 3, 4, 10, 40 };
    }
}
